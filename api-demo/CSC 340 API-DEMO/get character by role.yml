info:
  name: get character by role
  type: http
  seq: 13

http:
  method: GET
  url: http://localhost:8080/characters/by-role?role=Sensei
  params:
    - name: role
      value: Sensei
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
