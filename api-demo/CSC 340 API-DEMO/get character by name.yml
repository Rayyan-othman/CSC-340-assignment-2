info:
  name: get character by name
  type: http
  seq: 14

http:
  method: GET
  url: http://localhost:8080/characters/search?name=nar
  params:
    - name: name
      value: nar
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
