info:
  name: get character id
  type: http
  seq: 16

http:
  method: GET
  url: http://localhost:8080/characters/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
