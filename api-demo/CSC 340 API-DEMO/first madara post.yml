info:
  name: first madara post
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/characters
  body:
    type: json
    data: |-
      {
        "name": "Madara Uchiha",
        "universe": "Naruto",
        "role": "Villain",
        "description": "Legendary Uchiha clan leader"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
